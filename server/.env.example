DATABASE_URL="postgresql://user:password@localhost:5432/plateview"
JWT_SECRET="change-me-to-a-long-random-string"
PORT=4000
CORS_ORIGIN="http://localhost:5173"
ADMIN_EMAIL="owner@example.com"
ADMIN_PASSWORD="change-me"
HF_TOKEN="hf_..."
# Optional: tokens from additional free HF accounts, rotated automatically
# when one account's daily TRELLIS quota runs out. Any subset is fine.
# HF_TOKEN_2="hf_..."
# HF_TOKEN_3="hf_..."
